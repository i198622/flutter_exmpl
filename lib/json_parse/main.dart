import 'package:flutter/material.dart';

class JsonParseEntry extends StatelessWidget {
  const JsonParseEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
    // return const MaterialApp(
    //   home: BaseScreen(),
    // );
  }
}



