import 'package:flutter/material.dart';

class FeedInnerScreen extends StatelessWidget {
  const FeedInnerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Feed inner'),
      ),
    );
  }
}
